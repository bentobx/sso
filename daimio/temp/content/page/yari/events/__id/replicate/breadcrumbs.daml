{{* (
  :pages {* (
    :events {* (
      :url
      :yari/events
      :title "Events"
    )}
    :event_id {* (
      :url
      :yari/events/{#id}
      :title "{@event.name}")}
    )}
    :current "Replicate")}
    | (__)
    | merge string %template/components/breadcrumbs
  }
